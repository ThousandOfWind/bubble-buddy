{
  "app": "Bubble Buddy",
  "note": "Curated install facts. Mirror packaging/installer.iss + build.ps1 when they change. Not auto-generated.",
  "repo": "https://github.com/ThousandOfWind/bubble-buddy",
  "download": {
    "releases_page": "https://github.com/ThousandOfWind/bubble-buddy/releases",
    "latest_release": "https://github.com/ThousandOfWind/bubble-buddy/releases/latest",
    "how": "Open latest_release and download the asset for your chosen platform/edition. If tools are available, use gh release download or curl to fetch the asset directly."
  },
  "platforms": {
    "windows": {
      "install_dir": "%ProgramFiles%\\BubbleBuddy",
      "user_data_dir": "%USERPROFILE%\\.bubble-buddy",
      "editions": [
        {
          "id": "azure",
          "label": "Azure (lean, cloud)",
          "installer": "BubbleBuddy-Setup-<version>.exe",
          "size": "small",
          "requires": "Azure OpenAI sign-in",
          "use_when": "smallest download, has Azure access"
        },
        {
          "id": "full",
          "label": "Full (bundles offline Whisper)",
          "installer": "BubbleBuddy-Full-Setup-<version>.exe",
          "size": "large",
          "requires": "nothing (works offline)",
          "use_when": "no cloud account, privacy, fully offline"
        }
      ],
      "wizard_config_choices": [
        {"index": 0, "label": "Import an existing config.json", "shows_language_page": false},
        {"index": 1, "label": "Basic setup (enter Azure endpoint now)", "shows_language_page": true},
        {"index": 2, "label": "Skip - configure in the app (default)", "shows_language_page": true}
      ],
      "reinstall_preserves_config": "skip-path-only",
      "reinstall_config_note": "Default 'Skip' never overwrites an existing config.json (FileExists guard). 'Basic setup' and 'Import' replace it."
    },
    "macos": {
      "install_dir": "/Applications/Bubble Buddy.app",
      "user_data_dir": "~/.bubble-buddy",
      "editions": [
        {
          "id": "azure",
          "label": "Azure (lean, cloud)",
          "installer": "BubbleBuddy-<version>.dmg",
          "size": "small",
          "requires": "Azure OpenAI sign-in",
          "use_when": "smallest download, has Azure access"
        },
        {
          "id": "full",
          "label": "Full (local inference dependencies)",
          "installer": "BubbleBuddy-Full-<version>.dmg",
          "size": "large",
          "requires": "downloads model weights on demand",
          "use_when": "wants local MLX / faster-whisper transcription"
        }
      ],
      "install_steps": [
        "Open the DMG.",
        "Drag Bubble Buddy.app to /Applications.",
        "Launch Bubble Buddy.app.",
        "On first launch, use the Settings panel to pick backend/model/Azure settings."
      ],
      "model_note": "The Full DMG includes local inference dependencies, but not model weights. Users choose an MLX model path/repo in Settings; the model downloads on demand.",
      "agent_actions": [
        "Use gh release download --repo ThousandOfWind/bubble-buddy --pattern 'BubbleBuddy-Full-*.dmg' for local-model mac installs.",
        "Attach the DMG with hdiutil attach.",
        "Copy Bubble Buddy.app to /Applications.",
        "Write ~/.bubble-buddy/config.json if the user wants a specific backend/model.",
        "Open /Applications/Bubble Buddy.app and verify it starts."
      ],
      "reinstall_preserves_config": "existing-user-config",
      "reinstall_config_note": "The packaged app seeds ~/.bubble-buddy/config.json only when missing; existing config is preserved and merged with new missing defaults."
    }
  },
  "azure_setup": {
    "when": "Azure edition, or any install where speech.backend or polish.engine is azure.",
    "endpoint_is_secret": false,
    "config_path": {
      "windows": "%USERPROFILE%\\.bubble-buddy\\config.json",
      "macos": "~/.bubble-buddy/config.json"
    },
    "agent_actions": [
      "Ask the user only for their Azure OpenAI endpoint (https://<resource>.cognitiveservices.azure.com/); never ask for an API key.",
      "Write/merge config.json: speech.backend=azure, polish.engine=azure, azure.endpoint=<endpoint>, azure.auth=aad, plus the user's hotkey and language_preference.",
      "Restart the app so it reloads config.",
      "Hand off the sign-in: it needs an interactive browser the user completes; there is no CLI sign-in command."
    ],
    "signin_button": {
      "label_en": "🔑 Sign in to Azure",
      "label_zh": "🔑 登录 Azure",
      "location": "In the EXPANDED overlay panel (click the pet/orb to expand). Hidden while collapsed and hidden once signed in — not inside a separate Settings dialog.",
      "on_success": "Overlay shows 'Signed in to Azure'; auth record persists at ~/.bubble-buddy/auth_record.json and survives restarts."
    },
    "deployments_note": "transcribe_deployment defaults to gpt-4o-transcribe and chat_deployment to gpt-4.1; override if the user's Azure resource uses different deployment names."
  },
  "update": {
    "how": "Download the latest installer/DMG of the same edition and run it over the existing install.",
    "before": "Fully quit Bubble Buddy so the executable/app bundle is not locked.",
    "preserves_config": "Windows: use the default 'Skip' path on the config page to keep config.json; Basic/Import overwrite it. macOS: replacing the app preserves ~/.bubble-buddy/config.json."
  },
  "launch_at_startup": {
    "setting": "launch_at_startup",
    "ui": "Settings > General > Launch at startup",
    "windows_mechanism": "HKCU Run entry 'BubbleBuddy'"
  },
  "uninstall": {
    "how": "Windows: Settings > Apps, or the Start-menu uninstaller. macOS: quit the app and delete /Applications/Bubble Buddy.app.",
    "leftover_user_data": "~/.bubble-buddy is not removed; delete manually for a clean wipe."
  }
}
